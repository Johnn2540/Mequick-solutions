<div class="row">
  <div class="col-lg-6">
    <form method="POST" action="{{formAction}}" enctype="multipart/form-data">
      {{> csrf}}
      <div class="card border-0 shadow-sm">
        <div class="card-body">
          <div class="mb-3">
            <label for="name" class="form-label">Category Name *</label>
            <input type="text" id="name" name="name" class="form-control" value="{{category.name}}" required>
          </div>

          <div class="mb-3">
            <label for="description" class="form-label">Description</label>
            <textarea id="description" name="description" class="form-control" rows="4">{{category.description}}</textarea>
          </div>

          <div class="mb-3">
            {{#if category.image}}
              <img src="{{category.image}}" class="admin-image-preview d-block mb-2" alt="{{category.name}}">
            {{/if}}
            <label for="image" class="form-label">{{#if category.image}}Replace Image{{else}}Category Image{{/if}}</label>
            <input type="file" id="image" name="image" class="form-control" accept="image/png,image/jpeg,image/webp,image/avif">
          </div>

          <button type="submit" class="btn btn-primary px-4">{{#if category}}Update Category{{else}}Create Category{{/if}}</button>
          <a href="/admin/categories" class="btn btn-outline-secondary px-4">Cancel</a>
        </div>
      </div>
    </form>
  </div>
</div>
